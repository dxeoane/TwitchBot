debug = true

ssl {
  // for debugging purposes only
  trustAllCerts = false
}

twitch{
  irc {
    server = "irc.chat.twitch.tv"
    port = 6697
    capabilities = null // ":twitch.tv/tags twitch.tv/commands"

    anonymous = false
    channel= "dxeoane"

    // Only for non-anonymous connections
    token = "oauth:config_me"
    username = "dxeoane"
  }
}

// Include passwords and tokens
include "secret.conf"